package com.myhaimi.sms.modules.exam.entity.enums;

public enum ExamApplicableScopeType {
    /** Applies to all students / subjects in the school. */
    SCHOOL,
    /** Applies to all sections of a given grade/class. */
    CLASS,
    /** Applies to a specific class section. */
    SECTION,
    /** Applies to a specific subject. */
    SUBJECT
}
