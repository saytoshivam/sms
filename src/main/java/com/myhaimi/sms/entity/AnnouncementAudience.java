package com.myhaimi.sms.entity;

public enum AnnouncementAudience {
    /** Visible to everyone in the school (created by school admin / principal). */
    SCHOOL_WIDE,
    /** Visible only to students in linked class groups (created by teachers). */
    CLASS_TARGETS
}
