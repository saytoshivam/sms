package com.myhaimi.sms.entity.enums;

/** Nature of the employment arrangement. */
public enum EmploymentType {
    FULL_TIME,
    PART_TIME,
    CONTRACT,
    /** Visiting faculty — paid per lecture, no fixed schedule. */
    VISITING
}
