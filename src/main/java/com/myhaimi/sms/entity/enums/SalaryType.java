package com.myhaimi.sms.entity.enums;

/** Payroll calculation basis for a staff member. */
public enum SalaryType {
    MONTHLY,
    HOURLY,
    /** Remunerated per delivered lecture — typically for VISITING employment type. */
    PER_LECTURE
}
