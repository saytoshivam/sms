package com.myhaimi.sms.entity.enums;

/** Whether a document is required, optional, or explicitly not required for a school. */
public enum DocumentRequirementStatus {
    REQUIRED,
    OPTIONAL,
    NOT_REQUIRED
}

